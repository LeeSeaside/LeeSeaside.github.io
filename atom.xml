<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://leeseaside.github.io/</id>
    <title>LeeSeaside&apos;s Blog</title>
    <updated>2021-09-27T08:22:14.377Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://leeseaside.github.io/"/>
    <link rel="self" href="https://leeseaside.github.io/atom.xml"/>
    <subtitle>山本无忧，因雪白头。水本无愁，因风起皱。</subtitle>
    <logo>https://leeseaside.github.io/images/avatar.png</logo>
    <icon>https://leeseaside.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, LeeSeaside&apos;s Blog</rights>
    <entry>
        <title type="html"><![CDATA[IDEA 之 常用快捷键😮]]></title>
        <id>https://leeseaside.github.io/post/idea-zhi-chang-yong-kuai-jie-jian/</id>
        <link href="https://leeseaside.github.io/post/idea-zhi-chang-yong-kuai-jie-jian/">
        </link>
        <updated>2021-09-23T12:21:48.000Z</updated>
        <summary type="html"><![CDATA[<p>🚩 一些常用的idea快捷键，方便查阅，随时更新。</p>
]]></summary>
        <content type="html"><![CDATA[<p>🚩 一些常用的idea快捷键，方便查阅，随时更新。</p>
<!-- more -->
<hr>
<p><code>Ctrl</code> + <code>Shift</code> + A ：全局搜索设置</p>
<p><code>Shift</code> + <code>Shift</code> ：全局搜索</p>
<p><code>Ctrl</code> + <code>Shift</code> + 向右（左）箭头 ：选择</p>
<p><code>Alt</code> + <code>Shift</code> + 向上（下）箭头：上或下整行选择</p>
<p><code>Ctrl</code> + 斜杠 ：注释代码</p>
<p><code>Ctrl</code> + <code>Shift</code> + 斜杠 ：写注释</p>
<p><code>Alt</code> + 向下箭头 ：移动代码</p>
<p><code>Ctrl</code> + <code>NumPad -</code> ：折叠代码</p>
<p><code>Ctrl</code> + <code>NumPad +</code> ：展开代码</p>
<p><code>Ctrl</code> + <code>NumPad *</code> ：展开所有代码</p>
<p><code>Ctrl</code> +  <code>Shift</code> + <code>NumPad /</code> ：折叠所有代码</p>
<p><code>Alt</code> + <code>Shift</code> + Z ：增加环绕代码（for | if | try/catch）</p>
<p><code>Alt</code> + R ：取消环绕代码</p>
<p><code>Alt</code> + Y ：选择相同名字的代码（单个）</p>
<p><code>Alt</code> + <code>Shift</code> + ：取消相同名字的代码（单个）</p>
<p><code>Ctrl</code> +  <code>Shift</code> + Y ：选择所有相同名字的代码</p>
<p><code>Ctrl</code> +  <code>Shift</code> + <code>Enter</code> ：添加括号（;）结束代码</p>
<p><code>Alt</code> + <code>Shift</code> + R ：替换所有用到的变量</p>
<p><code>Alt</code> + <code>Shift</code> + L ：添加该行语句的变量</p>
<p><code>Alt</code> + <code>Shift</code> + M ：提取代码块，封装成一个方法</p>
<p><code>Ctrl</code> + <code>Alt</code>  + C ：将该字符变为公共变量</p>
<p><code>Ctrl</code> + <code>Alt</code>  + P ：将表达式提取为参数</p>
<p><code>Ctrl</code> + <code>Alt</code> + L ：格式化代码</p>
<p><code>Alt</code> + <code>Shift</code> + O ：高亮选择的所有使用变量</p>
<p><code>F3</code> ：跳转至类或接口的声明</p>
<p><code>Ctrl</code> + T ：跳转至该类或接口的实现</p>
<p><code>F12</code> ：跳转至源代码</p>
<p><code>Ctrl</code> + <code>F3</code> ：查看该类的代码结构</p>
<p><code>Ctrl</code> + K ：查找该变量被使用的下一个</p>
<p><code>Ctrl</code> +  <code>Shift</code> + K ：查找该变量被使用的上一个</p>
]]></content>
    </entry>
</feed>